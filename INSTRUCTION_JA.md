# Excel Query Trigger Manager — クイック手順

## 1. インストール

**初回利用前に、重要な Excel ファイルを必ずバックアップしてください。**
Dashboard を閉じても終了せず、プログラムはタスクトレイに常駐します。

**`Setup.cmd`** をダブルクリックし、表示されるウィンドウの指示に従ってください。
インストールはこれだけです。

インストール先は `%LOCALAPPDATA%\ExcelQueryTrigger`、スタートメニューにも登録されます。
Windows にサインインしたときの自動起動も、その画面で選べます。管理者権限は不要です。

更新するときも同じ `Setup.cmd` を実行してください。ルール・履歴・ログはそのまま残ります。
アンインストールも同じ画面から行えます。

## 2. 最初のファイルを追加する

**アプリの表示はすべて英語です。**

Trigger Rules の **Add** を押すと、3画面のウィザードが開きます。

1. いつ更新するか。
2. どのExcelブックを更新するか。
3. Rule名と設定内容が正しいか。

通常はこれで完了です。**Edit** で詳細Rule Editorを開けます。最初から全項目を
設定する場合は、一覧を右クリックして **New rule with all options...** を選びます。

### 一覧の見方

DashboardはRuleごとに1行表示します。Name、Trigger、Workbook、Where、Status、
Queries、Last Run、Next Run、Duration、Data updated を確認できます。フルパスや複数Workbookの詳細は
行のツールチップと右クリックメニューから確認できます。

## 3. 使い始める前に知っておくこと

### 起動していない間の変化は追跡しません

監視はアプリが起動してから始まります。

例：20:00 にアプリを終了 → 22:00 に監視フォルダーへ CSV が追加 →
翌朝 08:30 に起動。この CSV はトリガーになりません。

### 更新中も他の Excel は使えます

更新は専用の非表示 Excel インスタンスで行います。ただし、
**更新中のブックそのものを手動で開いたり編集したりしないでください。**

### Warn after は警告時間であって強制終了ではありません

この時間を超えても、アプリは Excel の完了を待ち続けます。停止したい場合は
**Cancel Job** を使います。これは「停止要求」です。

Excel がまったく応答しない場合は、待機中のダイアログを自動で閉じ、それでも
解決しなければ、その Excel プロセスを終了するかを確認します。詳しくは
`docs/MANUAL_JA.md` の「Cancelが効かない場合」を参照してください。

Excelが **Saving** / **Closing** の間は、書込み途中の破損を防ぐためCancelとExitを
受け付けません。安全な処理が完了するまで待ってください。

### 更新が失敗したときは

ルールを右クリック →**Test data sources** で、各ブックの接続先を Excel を開かずに
確認できます。サーバーに到達できるか、どこにも読み込まれていない接続かが分かります。

### 一覧の高さとバージョン確認

**Trigger Rules** タイトル右端の **Show all / Compact** で、一覧の高さを自動調整できます。
表示モードは次回起動時にも復元されます。
Ruleの行はドラッグして並べ替えできます。変更した順序は自動保存され、次回起動時にも維持されます。
各列の見出しをクリックすると昇順、もう一度クリックすると降順に並べ替えられます。
列ソートは表示上のみで、保存した手動順序は変更しません。

左下のバージョン番号をクリックすると、GitHub の公開 Release にある最新版を確認できます。
GitHub アカウントやトークンは不要で、Issue作成や報告送信は行いません。

## 4. 詳細

ダッシュボード右上の **i** ボタン、または `docs/MANUAL_JA.md` を参照してください。
